namespace Application
{
    public partial class DynamicProgrammingSolution
    {
        public int ClimbStairs(int n)
        {
            // if (n == 1) return 1;
            // var arr = new int[n];
            // Array.Fill(arr, 1);
            // var steps = 0;
            // var index = 1;
            // int maxIndex;
            // if (n % 2 == 1)
            // {
            //     maxIndex = arr.Length - 2;
            //     steps++;
            // }
            // else maxIndex = arr.Length - 1;
            // while (index <= maxIndex)
            // {
            //     var subSteps = 1 + (arr.Length - 1 - index);
            //     steps += subSteps;
            //     index += 2;
            // }
            // return steps + 1;
        }
    }
}