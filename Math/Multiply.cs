using System.Text;

namespace Application;
public partial class MathSolution
{
    public string Multiply(string num1, string num2)
    {
        // var sumList = new List<string>();
        // var remain = 0;
        // var multiplyResult = 0;
        // foreach (var item in num1)
        // {
        //     var num1Int = int.Parse(item.ToString());
        //     var sb = new StringBuilder();
        //     foreach (var item2 in num2)
        //     {
        //         var num2Int = int.Parse(item2.ToString());
        //         num1Int
        //     }
        // }
        return string.Empty;
    }
}